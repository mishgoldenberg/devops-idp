name: Service Backend CI

on:
  push:
    paths:
      - "backend/app/**"
      - "deployment/**"
      - ".github/workflows/**"
  workflow_dispatch:

# Prevent concurrent Helm upgrades against the same release.
# cancel-in-progress: false queues the second run instead of dropping it.
concurrency:
  group: helm-deploy-prod
  cancel-in-progress: false

jobs:
  backend:
    uses: ./.github/workflows/template-workflow.yml
    with:
      service_path: backend/app
      image_name: backend
      gcp_project: devops-idp-489012
      gcp_region: europe-west1
    secrets:
      GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      REDIS_PASSWORD: ${{ secrets.REDIS_PASSWORD }}
      JWT_SECRET: ${{ secrets.JWT_SECRET }}
      OAUTH_CLIENT_SECRET: ${{ secrets.OAUTH_CLIENT_SECRET }}
      OAUTH_CLIENT_ID: ${{ secrets.OAUTH_CLIENT_ID }}
      AZURE_DEVOPS_PAT: ${{ secrets.AZURE_DEVOPS_PAT }}
      AZURE_DEVOPS_ADMIN_PAT: ${{ secrets.AZURE_DEVOPS_ADMIN_PAT }}
      SERVICENOW_URL: ${{ secrets.SERVICENOW_URL }}
      SERVICENOW_USERNAME: ${{ secrets.SERVICENOW_USERNAME }}
      SERVICENOW_PASSWORD: ${{ secrets.SERVICENOW_PASSWORD }}